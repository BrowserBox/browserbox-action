name: Smoke Test BrowserBox Action
on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Run BrowserBox and Smoke Test
        id: bbx
        uses: ./
        with:
          license-key: ${{ secrets.BBX_LICENSE_KEY }}
          timeout: 3 # Enough time for startup + test

      - name: Verify Summary
        run: |
          echo "Login Link: ${{ steps.bbx.outputs.login-link }}"
          if [[ -z "${{ steps.bbx.outputs.login-link }}" ]]; then
            echo "Error: Login link is empty"
            exit 1
          fi
